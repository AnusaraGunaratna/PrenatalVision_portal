name: Deploy Portal to Azure

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
# Trigger on any push to main or any PR against main

jobs:
  build_and_deploy:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event_name != 'pull_request_target')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Build and Deploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "/" # Code is at root of this repo
          output_location: "dist"
        env:
          VITE_API_BASE_URL: https://prenatalvision-server.azurewebsites.net/api/v1
