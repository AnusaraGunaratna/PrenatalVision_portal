# PRENATAL_VISION PORTAL

1. Install dependencies: npm install
2. To update sample images:
   - Run the command: npm run updatesamples
3. Start the application: npm run dev
4. The application runs at http://localhost:5173 by default.
